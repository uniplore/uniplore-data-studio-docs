//Script here 
var Feature;
var Description;
var regNumber = /\d+/; //验证0-9的任意数字最少出现1次
var regString = /[a-zA-Z]+/; //验证大小写26个字母任意字母最少出现1次
 
   if(regNumber.test(text) == true){
          Feature = text.substring(2);
          Description = text.substring(0,1);
   }
   else
if(text.indexOf('Master Bedroom') > -1){
          Feature = 'Master Bedroom';
          Description
= text.substring(20);
   }
   else
if(text.indexOf('Main Bathroom') > -1){
          Feature
= 'Main Bedroom';
          Description
= text.substring(19);
   }
   else
if(text.indexOf('More Features:') > -1){
          Feature
= text.substring(15);
          Description
= 'Yes';
   }
   else
if(text == 'null'){
          Feature
= 'null';
          Description
= 'null';
   }
   else
if(text.indexOf('Style:')>-1){
          Feature
= 'Style';
          Description
= text.substring(7);
   }
   else{
          Feature
= text;
          Description
= 'Yes';
   }
 
trans_Status =
CONTINUE_TRANSFORMATION;